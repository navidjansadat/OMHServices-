PORT=5000
NODE_ENV=production
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY
# Fallback only for environments where writes are allowed by RLS:
SUPABASE_ANON_KEY=YOUR_ANON_KEY
SUPABASE_STORAGE_BUCKET=omh-assets
# Comma-separated browser origins allowed for credentialed requests
FRONTEND_ORIGINS=http://localhost:5000,http://localhost:3000,https://your-domain.com
